# This file is superseded by github-pages.yml for GH Pages deployment.
# Kept as a no-op so old workflow references don't 404.
name: Deploy (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment handled by github-pages.yml"
